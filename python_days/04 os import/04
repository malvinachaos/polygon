#! /usr/bin/python3
from os import system as S
S("clear")
print("Мы закончили на странице 83. По техническим причинам мы пропускаем главу 'Циклы'. Приступаем к главе 'Модули'")
# Однако мы не можем пропустить лямбда-функции. синтаксис:
#   lambda <список аргументов>: <выражение, которое нужно преобразовать, возвращается его результат>
print((lambda x: x**2 -2)(50))
c = input("ENTER...")
S("clear")


import MESSAGE
MESSAGE.print_msg()

# Импортия модуль, выполняется весь код оттуда
import cls
import хитрость

c = input("ENTER TO END...")
