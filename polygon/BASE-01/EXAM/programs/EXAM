#! /usr/bin/python3
'''
Написать программу, решающую следующую задачу: имеются 10 гирь весом 100, 200,
300, 500, 1000, 1200, 1400, 1500, 2000 и 3000 г. Сколькими способами гирями
этого набора можно составить вес в M грамм.
Пользователь вводит вес M в граммах. В качестве ответа программа выводит
количество способов, которыми можно составить вес. Например, для M = 300,
ответ будет 2 (100 + 200 и 300).
'''
# Их 10
fullmass = (100, 200, 300, 500, 1000, 1200, 1400, 1500, 2000, 3000)
# M -- пользовательское значение
# x -- индекс массива fullmass
# i -- количество способов
# N -- для расчётов
M, x, i = 0, 9, 10
# Блок ввода. Нужно убедится, что вводимое пользователем зачение -- число
while True:
    M = input('Введите вес в граммах: ')
    if not M.isdigit:
        print('\nВы неверно ввели данные!!!')
        continue
    M = int(M)
    break

# Определение ближайшего максимума
while M - fullmass[x] < 0 and x >= 0:
    x -= 1

while x >= 0:
    n = fullmass[x]
    print('----------------------------')
    for j in range(x-1, -1, -1):
        print(f'\n {j})\t {n} {fullmass[j]}')
        n -= fullmass[j]
        print(f'\t\t {n} {fullmass[j]}')
        if n == 0 or n + fullmass[j] == M:
            i+= 1
            print(f'\t\t {n} {fullmass[j]}')
            print("Y E S")
            break
        elif n < 0:
            break
    print('----------------------------')
    x -= 1

print('Количество способов:', i)
